You need to import whisper using 
      --  pip install -U openai-whisper

For other requirements you can directly use 
      --  pip3 install -r requirements.txt

For Flask application to run use
    -- python3 "app.py-PATH"
